version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: yarn
      - run: yarn build
  # test:
  #   steps:
  #     - checkout
  #     - run: yarn
  #     - run: yarn test
  cypress:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: docker-compose -f dev-docker-compose.yml up --build
      - run: yarn cy:run
  
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      # - test
      - cypress